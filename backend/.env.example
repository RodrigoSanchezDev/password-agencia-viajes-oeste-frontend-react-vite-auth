# Configuración del servidor
PORT=3001

# Secreto para JWT (en producción usar variable de entorno segura)
JWT_SECRET=your_secret_key_here

# Tiempo de expiración del token (ejemplo: 1h, 24h, 7d)
JWT_EXPIRES_IN=24h
